fixtures:
  # use the forge modules so we can easily pin to a specific version
#  forge_modules:
#    stdlib: "puppetlabs/stdlib"
  repositories:
    stdlib: "git://github.com/puppetlabs/puppetlabs-stdlib.git"
    selinux:
      repo: "git@github.com:jfryman/puppet-selinux.git"
      ref: "v0.2.2"
  symlinks:
    cobbler: "#{source_dir}"
